# Optional: enables LLM-generated narrative insights via Claude.
# Without this set, the app falls back to rule-based recommendation text.
ANTHROPIC_API_KEY=

# Comma-separated list of allowed frontend origins for CORS
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
